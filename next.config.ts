import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Nota: El error sobre funciones de color "lab" es una advertencia de Turbopack
  // que no afecta la funcionalidad. El navegador procesa correctamente estos colores.
};

export default nextConfig;
